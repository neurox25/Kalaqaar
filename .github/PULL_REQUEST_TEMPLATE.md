### Summary

Short description of the change and why it was made.

### Files changed

List the key files/paths the PR touches (e.g. `services/functions/src/payments`, `config.js`, `featureFlags.js`).

### Testing

- Which config/env was used to validate (e.g. `staging`)
- Unit tests run: (yes/no)
- Emulators used: (yes/no)
- Short notes on test evidence or screenshots/log snippets.

### Safety & Secrets

- Any new secrets required? (names only)
- Any changes to payouts/ledger logic? (if yes: include emulators E2E evidence)

### Notes for reviewers

- Where to look for logic (e.g. `services/functions/src/payouts`)
- Anything the reviewer should validate specially
